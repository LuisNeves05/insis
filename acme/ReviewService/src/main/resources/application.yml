server:
  port: 8084
spring:
  application:
    name: ReviewService
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5433/reviewService
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
