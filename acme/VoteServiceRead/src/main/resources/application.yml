server:
  port: 8087
spring:
  application:
    name: VoteServiceRead
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5677
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5438/vote-db-r
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
