server:
  port: 8083
spring:
  application:
    name: ProductService
  datasource:
    password: password
    url: jdbc:postgresql://192.168.1.66:5432/product-db
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true


