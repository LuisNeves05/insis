server:
  port: 8083
spring:
  application:
    name: ReviewServiceRead
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5435/review-db-r
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
