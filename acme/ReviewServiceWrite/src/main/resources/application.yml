server:
  port: 8084
spring:
  application:
    name: ReviewServiceWrite
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    port: 5677
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5434/review-db-w
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
